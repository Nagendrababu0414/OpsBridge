@isTest
public class EventSelectorTest {
@isTest
    public static void eventSelectorTest(){
        EventSelector evt = new EventSelector();
        string eventId;
        evt.eventList(eventId);
        string evid;
        evt.getevent(evid);
        EventSelector.getInstance();
    }
}